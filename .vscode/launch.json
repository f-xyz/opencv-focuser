{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Focuser",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/focuser",
      "args": [],
      "cwd": "${workspaceFolder}",
      "targetArchitecture": "x64",
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "CMake: build",
      "setupCommands": [
        {
          "text": "-enable-pretty-printing",
          "description": "Enable pretty-printing for gdb",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
